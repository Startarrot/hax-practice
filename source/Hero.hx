class Hero extends FlxSprite
{
	override public function new()
	{
		super();
		makeGraphic(10, 10, FlxColor = FlxColor.BLUE)
	}

	override public function update(elapsed:Float)
	{
		super.update(elapsed);
	}
}
